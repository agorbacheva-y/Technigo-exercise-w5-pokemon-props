// Destructure pokemon types props here :)
/*export const Type = ({ types }) => {
  return (
    <div>
      <p>Type: {types.toString()}</p>
    </div>
  );
};

Type.defaultProps = {
  types: 'No types'
};*/